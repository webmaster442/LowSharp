<?xml version="1.0" encoding="utf-8" ?>
<examples>
  <e n="Record class" t="csharp">
    <![CDATA[
    record class ExampleRecord(string Name, int Value);]]>
  </e>
  <e n="Primary constructor" t="csharp">
    <![CDATA[
class PrimaryCtor(string name, int value)
{
	public int Property => value;
	
	public int Computed { get; private set; }
	
	public void SomeMethod()
	{
		Computed *= name.Length;
	}
}]]>
  </e>
  <e n="Sample F# lib" t="fsharp">
    <![CDATA[namespace MyLib

/// A simple utility module
module MathUtils =
    
    /// Adds two integers.
    let add a b = a + b

    /// Multiplies two integers.
    let multiply a b = a * b


/// A simple class with methods
type Greeter(name: string) =
    member _.Name = name

    member this.Greet() =
        $"Hello, {this.Name}!"
  ]]>
  </e>
</examples>